You are executing a queued automation task for the SocialPredict repository at /workspace/spec-socialpredict-tasks.

Start by explicitly spawning the custom agent named `dispatcher_agent` to coordinate the task. The dispatcher should decide whether to spawn any specialist agents and should wait for them before concluding.

Reporting convention:
- The canonical curated report location is `/workspace/socialpredict/.codex-reports/tasks/PR581-012`.
- Report files are already bootstrapped:
  - meta: `/workspace/socialpredict/.codex-reports/tasks/PR581-012/meta.json`
  - summary: `/workspace/socialpredict/.codex-reports/tasks/PR581-012/summary.json`
  - conversation log: `/workspace/socialpredict/.codex-reports/tasks/PR581-012/conversation.ndjson`
  - decisions log: `/workspace/socialpredict/.codex-reports/tasks/PR581-012/decisions.ndjson`
- Use helper script `/workspace/spec-socialpredict-tasks/scripts/codex-report.py` for deterministic report I/O when practical.
- Specialists should append facts and decisions; the dispatcher owns `summary.json`.
- Prefer incremental reads, for example `read-events --after-seq <last_event_seq>` instead of rereading the full conversation log.
- `summary.json.context` is runner-maintained. If `handoff_requested` becomes true, stop branching out, update the summary, record a handoff event, and converge toward a clean checkpoint.

Execution contract:
- Do the task end-to-end.
- Do not stop for clarifying questions unless blocked by a missing file, contradictory task instructions, or a safety issue.
- Prefer the smallest defensible change.
- Follow repository AGENTS.md guidance and nearby repo docs.
- Use machine-readable task metadata below as the source of truth.
- Finish with a concise summary that includes: task_id, status, files_changed, checks_run, and follow_ups.

Task metadata:
{
  "approval": "never",
  "attempts": 1,
  "depends_on": [
    "PR581-001"
  ],
  "id": "PR581-012",
  "max_attempts": 3,
  "notes": [
    "The file and symbol inventory in the prompt is the execution scope; the PR comments are provenance only.",
    "Keep this task focused on hidden global state, concrete DB coupling, login/auth boundary cleanup, and narrower sanitizer responsibilities rather than broad unrelated refactors.",
    "This task comes from the general review comment and cyclomatic-complexity follow-up rather than from the later OCP/LSP/ISP/DIP markdown tables."
  ],
  "profile": "",
  "prompt": "Work only in the remaining non-checklist hotspot files and exact symbols defined here. Reduce hidden global state and concrete coupling in util/postgres.go (DB, DBConfig, DBFactory, PostgresFactory, LoadDBConfigFromEnv, BuildPostgresDSN, PostgresFactory.Open, InitDB, GetDB, firstNonEmpty); tighten auth login flow boundaries in internal/service/auth/loggin.go (getJWTKey, UserClaims, LoginHandler, decodeLoginRequest, validateAndSanitizeLogin, authenticateUser, findUserByUsername, generateJWT, writeLoginResponse); and narrow sanitizer responsibilities in security/sanitizer.go (NewSanitizer, createBasicPolicy, SanitizeUsername, SanitizeDisplayName, SanitizeDescription, SanitizeMarketTitle, SanitizePersonalLink, validatePersonalLinkLength, parseURLWithScheme, validateAllowedScheme, SanitizeEmoji, SanitizePassword, CheckPasswordLength, CheckPasswordChars, containsSuspiciousPatterns, containsMaliciousDomain, isValidEmoji, isEmojiRune, isASCIIEmojiRune, hasUppercase, hasLowercase, hasDigit). Resolve the remaining boundary and validation issues in those exact symbols without weakening existing protections.",
  "runner_pid": 40595,
  "sandbox": "workspace-write",
  "started_at": "2026-04-04T13:59:25.920771+00:00",
  "status": "running",
  "title": "Clean up remaining dependency-boundary hotspots outside the checklists",
  "working_dir": "../socialpredict/backend"
}

